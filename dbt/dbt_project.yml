name: "neuro_sleep"
version: "1.0.0"
config-version: 2

profile: "neuro_sleep"

model-paths:
  - "models"

test-paths:
  - "tests"

target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"
